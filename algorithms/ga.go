package algorithms

import (
	"knapsackProblem/model"
	"math/rand"
)

func GAResolver(noGeneration, maxCapacity int, mutationRate float32, knapsack []model.KnapsackItem) {
	
}

func initPopulation(knapsack []model.KnapsackItem, populationSize int) {
	rand.Intn(1)
}